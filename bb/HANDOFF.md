# HANDOFF

## Current State

- 실행 기준은 `bb/server.ps1` + `milkit` 정적 서버 조합이다.
- 화면 계약은 이벤트 로그 상단 분리, 좌측 플레이어/우측 컴퓨터 추측 로그 2열, `thinkingNotice`, 컴퓨터 로그 토글 기본 `켜짐`, 좌상단 메모 카드 4칸을 한 세트로 본다.
- 난이도 계약은 `bb/SPEC.md` 기준이며, 특히 `3단계`는 `script.js`의 후보군 기반 샘플링 검색 로직을 사용한다.

## Open Notes

- 현재 추가 인수인계는 없다. 기능 변경이 생기면 연대기 로그 대신 현재 동작 차이만 이 파일에 갱신한다.
